1. the blog_code.R file has the code to pre-process the downloaded csv files as explained in the report
2. the data_multi_class.zip has data files for the multi-class classification. The code for the same is available as a
iPython Notebook which is attaced as flightweather_delaypred.ipynb
3. the package RandomDataSet.zip has code and dataset files for the Logistic Regression and RandomForest classifier
for the random data (random based on source and destination of flights)